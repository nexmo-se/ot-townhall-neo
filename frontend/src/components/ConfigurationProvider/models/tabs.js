class Tabs {
  constructor (args) {
    this.questions = args.questions;
    this.participants = args.participants;
    this.polling = args.polling;
    this.chat = args.chat;
  }
}

export default Tabs;
